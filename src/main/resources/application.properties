spring.application.name=tree-pruning

# ─── PostgreSQL ───────────────────────────────────────────────────────────────
<<<<<<< Updated upstream
# Local: usa los defaults (:valor). Desplegado: Infisical inyecta las variables.
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:5433/${POSTGRES_DB:treepruning}
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:postgres123}
=======
# Todas las vars vienen del entorno (inyectadas por `infisical run` en runtime).
# NO hay defaults — si falta cualquier var, Spring falla al arrancar con un
# error claro tipo "Could not resolve placeholder 'POSTGRES_DB'".
# Para correr local: exportar las vars antes (`infisical run -- ./mvnw spring-boot:run`).
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST}:5432/${POSTGRES_DB}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
>>>>>>> Stashed changes
spring.datasource.driver-class-name=org.postgresql.Driver

# ─── JPA / Hibernate ──────────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO}
spring.jpa.show-sql=${JPA_SHOW_SQL}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
<<<<<<< Updated upstream
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}
spring.jpa.properties.hibernate.default_schema=${JPA_SCHEMA:public}
spring.jpa.open-in-view=false

# ─── Server ───────────────────────────────────────────────────────────────────
server.port=${SERVER_PORT:8080}

# --- Actuator ---
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.metrics.tags.application=tree-pruning
management.metrics.tags.environment=local
info.app.name=TreePruning
info.app.version=1.0.0
=======
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL}
spring.jpa.properties.hibernate.default_schema=${JPA_SCHEMA}
spring.jpa.open-in-view=false

# ─── Server ───────────────────────────────────────────────────────────────────
server.port=${SERVER_PORT}
>>>>>>> Stashed changes
